{
  "name": "cortex-daemon",
  "version": "0.1.0",
  "description": "Background daemon service for Cortex note-taking system",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --format esm --target bun",
    "dev": "bun --hot ./src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "cortex-core": "workspace:*",
    "chokidar": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}